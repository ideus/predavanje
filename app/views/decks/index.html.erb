<% @decks.each do |deck| %>
  <div>
    <%= link_to deck.name, "/decks/#{deck.id}" %>
    -
    <%= link_to "Edit", "/decks/#{deck.id}/edit" %>
    <%= link_to "Delete", "/decks/#{deck.id}", method: :delete %>
    
  </div>
<% end %>
<%= link_to "New Deck", "/decks/new" %>